services:
  mongodb:
    image: mongo:6
    restart: always
    ports:
      - 27017:27017
    environment:
      MONGO_INITDB_DATABASE: movies
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
